# Optional Codex adapter for model-aware specialist routing.
# The Markdown and Git workflow remains usable when Codex is unavailable.

# The root agent coordinates routing and ordinary work.
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"

[agents]
# Keep delegation bounded and prevent recursive fan-out.
max_threads = 3
max_depth = 1
