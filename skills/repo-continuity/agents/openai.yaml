interface:
  display_name: "Repo Continuity"
  short_description: "Route work, preserve knowledge, and improve the workflow"
  default_prompt: "Use $repo-continuity to route this task, load only needed project knowledge, preserve recoverable context at meaningful boundaries, write back reusable experience idempotently, and capture evidence-backed template friction only when it occurs."
